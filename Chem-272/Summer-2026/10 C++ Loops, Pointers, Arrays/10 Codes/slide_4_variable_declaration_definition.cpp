int main() {
  // "Declare" myVar.
  // It exists but doesn't have a meaningful value
  int myVar;

  // Define myVar to be 10
  myVar = 10; 

  // We usually do this all at once
  int myVar = 10;

  return 0;
}